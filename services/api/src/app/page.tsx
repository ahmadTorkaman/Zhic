export default function Page() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem', lineHeight: 1.6 }}>
      <h1>Zhic API — skeleton</h1>
      <p>
        This service reserves the <code>api.zhicwood.com</code> boundary. Payload 3, collections,
        the Postgres adapter, and the admin UI at <code>/admin</code> are wired in Session 1.3.
      </p>
    </main>
  );
}
